# Copy to .env (gitignored) and fill in real values.
# All JSON configs reference these via {"type": "environment", "value": "VAR_NAME"}.
# ⚠️ The old hardcoded keys in this repo's git history are burned — use NEW keys only.

# ── RPC URLs: chains the Monitor watches ─────────────────────────────────────
# Ethereum mainnet: primary Monitor RPC.
RPC_ETHEREUM_MAINNET=
# Optional private fallbacks. Keep the real URLs only in `.env`.
RPC_ETHEREUM_MAINNET_FALLBACK_BLOCKPI=
RPC_ETHEREUM_MAINNET_FALLBACK_DRPC=
# Sepolia: Infura with a NEW project key. `check-rpcs` treats this template
# placeholder as missing until it is replaced with a real key.
RPC_SEPOLIA=https://sepolia.infura.io/v3/YOUR_NEW_INFURA_KEY

# ── RPC URLs: EVMCall target chains (used by the unified NewReport handler) ──
# These replay submitted EVMCall values at the reported historical block only.
# They do not make Monitor watch those chains unless matching network JSONs and
# monitor network slugs are added. Ethereum chain ID 1 uses the primary URL above.
RPC_POLYGON=https://polygon-mainnet.infura.io/v3/YOUR_NEW_INFURA_KEY
RPC_OPTIMISM=https://optimism-mainnet.infura.io/v3/YOUR_NEW_INFURA_KEY
RPC_GNOSIS=https://rpc.gnosis.gateway.fm
RPC_CHIADO=https://rpc.chiado.gnosis.gateway.fm

# ── Notifications ─────────────────────────────────────────────────────────────
# Production routes are read on every delivery from this host-relative path.
# Create secrets/ with mode 0700 and discord_webhooks.json with mode 0600.
DISCORD_WEBHOOKS_FILE=secrets/discord_webhooks.json
# The long-running service is live. quickstart replay explicitly overrides this
# to log-only unless --send is confirmed.
TELLOR_ALERT_DELIVERY_MODE=live
# Isolated webhook used only by quickstart.py's generated custom monitor.
# quickstart.py writes this securely and never puts the URL in generated JSON.
CUSTOM_DISCORD_WEBHOOK_URL=

# ── External APIs (rotate the burned keys first) ─────────────────────────────
# Optional 4th TellorFlex source (CoinGecko + Coinbase + DefiLlama work without keys).
CMC_PRO_API_KEY=
# Optional legacy helper key; the unified 17-feed catalog does not use CoinCap.
COINCAP_API_KEY=
# Required to verify the unified BRL/USD and CNY/USD SpotPrice reports.
EXCHANGERATE_API_KEY=

# ── Monitor runtime (optional overrides) ─────────────────────────────────────
# RUST_LOG=info
# METRICS_ENABLED=false
